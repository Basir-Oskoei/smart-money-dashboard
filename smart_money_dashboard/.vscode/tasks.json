{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Smart Money API",
            "type": "shell",
            "command": "uvicorn backend.main:app --reload",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
